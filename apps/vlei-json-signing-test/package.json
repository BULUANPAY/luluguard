{
  "name": "@repo/vlei-json-signing-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test-vlei": "pnpm --filter @repo/vlei-json-signing build && pnpm run build && node --env-file-if-exists=../../.env dist/test-signing.js"
  },
  "dependencies": {
    "@repo/vlei-json-signing": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^24.0.0",
    "eslint": "^10.0.0",
    "typescript": "7.0.2"
  }
}
